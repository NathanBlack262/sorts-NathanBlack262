

def insertion_sort():
    pass


def bubble_sort(arr,debug=False):
    whole_row = False
    while !whole_row:
        whole_row_counter = 0
        for i in range(0,len(arr)):
            a = arr[i]
            b = arr[i+1]
            if a >= b:
                arr[i] = b
                arr[i+1] = b
            elif b < a:
                arr[i] = a
                arr[i+1] = b
            else:
                whole_row_counter += 1
                if (whole_row_counter == len(arr) - 1):
                    
                
    
    
def selection_sort():
    pass
    
